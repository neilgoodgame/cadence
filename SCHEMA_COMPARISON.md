# Schema comparison: Python (`backend/`) vs Java (`backend_java/`)

Both backends implement the same `openapi.yaml` contract against logically the same data
model, but the actual Postgres schemas are not identical. This documents every real
difference, generated by diffing `information_schema.columns` from a freshly-migrated
instance of each backend — not by reading the model/entity source, so it reflects what's
actually in the database rather than what the code is supposed to produce.

## Table names

Django's default table-naming convention is `<app_label>_<modelname>` (e.g.
`activities_activity`); the Java side uses plain names matching the original Data
Dictionary directly via explicit `@Table(name = "...")` annotations on each JPA entity.
This is the most visible difference, but it's purely cosmetic — every pair below stores
the same logical entity.

| Java table | Python table |
|---|---|
| `users` | `accounts_user` |
| `user_relationship` | `accounts_userrelationship` |
| `personal_access_token` | `accounts_personalaccesstoken` |
| `activity` | `activities_activity` |
| `activity_tag` | `activities_activitytag` |
| `best_effort` | `activities_besteffort` |
| `duration_curve` | `activities_durationcurve` |
| `lap` | `activities_lap` |
| `record` | `activities_record` |
| `tag` | `activities_tag` |
| `zone_set` | `athletes_zoneset` |
| `bike` | `gear_bike` |
| `component` | `gear_component` |
| `service_record` | `gear_servicerecord` |
| `shoe` | `gear_shoe` |
| `shoe_model` | `gear_shoemodel` |
| `shoe_model_version` | `gear_shoemodelversion` |
| `scheduled_workout` | `scheduling_scheduledworkout` |
| `upload` | `uploads_upload` |
| `upload_batch` | `uploads_uploadbatch` |
| `webhook` | `webhooks_webhook` |
| `webhook_delivery` | `webhooks_webhookdelivery` |
| `workout` | `workouts_workout` |
| `workout_step` | `workouts_workoutstep` |

`oauth_authorization` (Java) has no single equivalent — see "Structural differences"
below. Every other table on each side (Django's auth/admin/session machinery, Java's
Spring Batch job-tracking tables, Flyway's own history table) has no counterpart at all;
see the tables at the bottom of this document.

## Key functional differences

These are the differences worth actually acting on — everything else below is naming or
schema-tightness noise with no behavioral effect.

- **Python captures per-second environmental/physiological telemetry that Java
  doesn't.** `record` has `air_temp`, `core_temp`, `heat_strain`, `humidity`, `skin_temp`
  on the Python side with no Java equivalent; `activity` similarly has `avg_air_temp` /
  `avg_humidity`. These come from the same Stryd developer fields the Java
  `FitFileParser` fix earlier only special-cased for `Power` (the Stryd dump in that
  investigation showed `core_temperature`, `skin_temperature`, `heat_strain_index`,
  `Stryd Humidity`, `Stryd Temperature` sitting right next to it). **The Java backend
  has fallen behind Python on this feature** — it was added to Python after the Java
  port was built, not omitted on purpose. If parity matters here, this is a real gap to
  close, not a naming issue.
- **`users.password` is nullable on Java, `NOT NULL` on Python.** Verified this has no
  actual effect — `UserService` always sets a bcrypt hash on registration, so the column
  is just looser than it needs to be, not a functional gap.
- Everything else (column renames, nullable-vs-not-null on a couple of enum columns,
  `VARCHAR` length differences) is listed in the per-entity tables below for completeness
  but doesn't change behavior.

## Per-entity column comparison

### `users` (Java) vs `accounts_user` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| age | integer | smallint | type differs (no functional effect) |
| critical_run_power | integer | integer | |
| date_joined | timestamptz NOT NULL DEFAULT now() | timestamptz NOT NULL | |
| email | varchar(255) NOT NULL | varchar(254) NOT NULL | type differs |
| ftp | integer | integer | |
| handle | varchar(50) | varchar(50) | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| is_active | boolean NOT NULL DEFAULT true | boolean NOT NULL | |
| is_coach | boolean NOT NULL DEFAULT false | boolean NOT NULL | |
| is_staff | — | boolean NOT NULL | Python only — Django `AbstractUser` field, not part of the product contract |
| is_superuser | — | boolean NOT NULL | Python only — same |
| last_login | — | timestamptz | Python only — same |
| lthr | integer | integer | |
| max_hr | integer | integer | |
| name | varchar(150) NOT NULL | varchar(150) NOT NULL | |
| password | varchar(255) | varchar(128) NOT NULL | nullable on Java but always set in practice (see above) |
| threshold_pace | varchar(10) NOT NULL DEFAULT '' | varchar(10) NOT NULL | |
| weight_kg | double precision | double precision | |

### `user_relationship` (Java) vs `accounts_userrelationship` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| created | timestamptz NOT NULL DEFAULT now() | timestamptz NOT NULL | |
| grantee_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| owner_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| role | varchar(10) NOT NULL | varchar(10) NOT NULL | |
| status | varchar(10) NOT NULL DEFAULT 'pending' | varchar(10) NOT NULL | |

### `personal_access_token` (Java) vs `accounts_personalaccesstoken` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| created | timestamptz NOT NULL DEFAULT now() | timestamptz NOT NULL | |
| expires_at | date | date | |
| hashed_secret | varchar(64) NOT NULL | varchar(64) NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| last_used | date | date | |
| name | varchar(150) NOT NULL | varchar(150) NOT NULL | |
| prefix | varchar(20) NOT NULL | varchar(20) NOT NULL | |
| scopes | jsonb NOT NULL DEFAULT '[]' | jsonb NOT NULL | |
| user_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |

### `activity` (Java) vs `activities_activity` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| ascent | integer | integer | |
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| avg_air_temp | — | double precision | **Python only — feature gap, see above** |
| avg_hr | integer | integer | |
| avg_humidity | — | integer | **Python only — feature gap, see above** |
| avg_power | integer | integer | |
| bike_id | varchar(40) | varchar(40) | |
| distance_km | double precision NOT NULL DEFAULT 0 | double precision NOT NULL | |
| distance_source | varchar(10) NOT NULL DEFAULT 'gps' | varchar(10) NOT NULL | |
| end_weight_kg | double precision | double precision | |
| environment | varchar(10) NOT NULL DEFAULT 'outdoor' | varchar(10) NOT NULL | |
| fluids_ml | integer | integer | |
| has_gps | boolean NOT NULL DEFAULT false | boolean NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| intensity | double precision | double precision | |
| max_hr | integer | integer | |
| moving_time | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| name | varchar(200) NOT NULL | varchar(200) NOT NULL | |
| norm_power | integer | integer | |
| shoe_id | varchar(40) | varchar(40) | |
| source | varchar(100) NOT NULL DEFAULT '' | varchar(100) NOT NULL | |
| sport | varchar(10) NOT NULL | varchar(10) NOT NULL | |
| start_date | timestamptz NOT NULL | timestamptz NOT NULL | |
| start_weight_kg | double precision | double precision | |
| tss | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| workout_id | varchar(40) | varchar(40) | |

### `activity_tag` (Java) vs `activities_activitytag` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| activity_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| id | bigint (sequence) | bigint | |
| tag_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |

### `best_effort` (Java) vs `activities_besteffort` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| activity_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| date | date NOT NULL | date NOT NULL | |
| id | bigint (sequence) | bigint | |
| kind | varchar(20) NOT NULL | varchar(20) NOT NULL | |
| unit | varchar(20) NOT NULL | varchar(20) NOT NULL | |
| value | double precision NOT NULL | double precision NOT NULL | |
| window / window_label | `window_label` | `window` | renamed only |

### `duration_curve` (Java) vs `activities_durationcurve` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| activity_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| extends_to | integer NOT NULL | integer NOT NULL | |
| id | bigint (sequence) | bigint | |
| metric | varchar(10) NOT NULL | varchar(10) NOT NULL | |
| points | jsonb NOT NULL | jsonb NOT NULL | |

### `lap` (Java) vs `activities_lap` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| activity_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| avg_hr | integer | integer | |
| avg_power | integer | integer | |
| distance_km | double precision NOT NULL | double precision NOT NULL | |
| duration | integer NOT NULL | integer NOT NULL | |
| id | bigint (sequence) | bigint | |
| index / lap_index | `lap_index` | `index` | renamed only — `index` isn't reserved in Postgres but Java avoided it anyway |

### `record` (Java) vs `activities_record` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| activity_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| air_temp | — | double precision | **Python only — feature gap, see above** |
| altitude | double precision | double precision | |
| cadence | integer | integer | |
| core_temp | — | double precision | **Python only — feature gap, see above** |
| distance_km | double precision | double precision | |
| heartrate | integer | integer | |
| heat_strain | — | double precision | **Python only — feature gap, see above** |
| humidity | — | integer | **Python only — feature gap, see above** |
| id | — (composite `activity_id, ts` PK) | bigint surrogate PK | structural — see "Structural differences" |
| lat / lng | double precision | double precision | |
| power | integer | integer | |
| skin_temp | — | double precision | **Python only — feature gap, see above** |
| speed | double precision | double precision | |
| t | integer NOT NULL | integer NOT NULL | |
| ts | timestamptz NOT NULL | timestamptz NOT NULL | |

### `tag` (Java) vs `activities_tag` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| color | varchar(20) NOT NULL DEFAULT '' | varchar(20) NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| name | varchar(100) NOT NULL | varchar(100) NOT NULL | |
| origin | varchar(10) NOT NULL DEFAULT 'manual' | varchar(10) NOT NULL | |

### `zone_set` (Java) vs `athletes_zoneset` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| id | bigint (sequence) | bigint | |
| type | varchar(20) NOT NULL | varchar(20) NOT NULL | |
| zones | jsonb NOT NULL | jsonb NOT NULL | |

### `bike` (Java) vs `gear_bike` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| distance_km | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| groupset | varchar(150) NOT NULL DEFAULT '' | varchar(150) NOT NULL | |
| hours | double precision NOT NULL DEFAULT 0 | double precision NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| kind | varchar(10) | varchar(10) NOT NULL | nullable on Java, not on Python |
| name | varchar(150) NOT NULL | varchar(150) NOT NULL | |
| rides | integer NOT NULL DEFAULT 0 | integer NOT NULL | |

### `component` (Java) vs `gear_component` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| bike_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| km | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| limit_km | integer NOT NULL | integer NOT NULL | |
| model | varchar(150) NOT NULL DEFAULT '' | varchar(150) NOT NULL | |
| name | varchar(150) NOT NULL | varchar(150) NOT NULL | |

### `service_record` (Java) vs `gear_servicerecord` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| action | varchar(10) | varchar(10) NOT NULL | nullable on Java, not on Python |
| component_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| date | date NOT NULL | date NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| note | varchar(500) NOT NULL DEFAULT '' | varchar(500) NOT NULL | |
| reset | boolean NOT NULL DEFAULT true | boolean NOT NULL | |

### `shoe` (Java) vs `gear_shoe` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| colourway | varchar(150) NOT NULL DEFAULT '' | varchar(150) NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| image | varchar(2048) | varchar(200) | Java allows longer URLs |
| km | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| limit_km | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| name | varchar(200) NOT NULL | varchar(200) NOT NULL | |
| retired | boolean NOT NULL DEFAULT false | boolean NOT NULL | |
| role | varchar(150) NOT NULL DEFAULT '' | varchar(150) NOT NULL | |
| shoe_model_version_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| since | date NOT NULL DEFAULT CURRENT_DATE | date NOT NULL | |

### `shoe_model` (Java) vs `gear_shoemodel` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| manufacturer | varchar(150) NOT NULL | varchar(150) NOT NULL | |
| model | varchar(150) NOT NULL | varchar(150) NOT NULL | |

### `shoe_model_version` (Java) vs `gear_shoemodelversion` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| shoe_model_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| version | varchar(50) NOT NULL | varchar(50) NOT NULL | |

### `scheduled_workout` (Java) vs `scheduling_scheduledworkout` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| activity_id | varchar(40) | varchar(40) | |
| assigned_by / assigned_by_id | `assigned_by` | `assigned_by_id` | Django auto-suffixes FK columns with `_id`; Java's JPA mapping didn't here |
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| date | date NOT NULL | date NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| status | varchar(10) NOT NULL DEFAULT 'planned' | varchar(10) NOT NULL | |
| time_of_day | varchar(3) | varchar(3) NOT NULL | nullable on Java, not on Python |
| workout_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |

### `upload` (Java) vs `uploads_upload` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| activity_id | varchar(40) | varchar(40) | |
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| batch_id | varchar(40) | varchar(40) | |
| completed_at | timestamptz | timestamptz | |
| error_code | varchar(100) NOT NULL DEFAULT '' | varchar(100) NOT NULL | |
| error_message | varchar(500) NOT NULL DEFAULT '' | varchar(500) NOT NULL | |
| file_hash | varchar(64) NOT NULL | varchar(64) NOT NULL | |
| filename | varchar(255) NOT NULL | varchar(255) NOT NULL | |
| fluids_ml | integer | integer | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| progress | double precision | double precision NOT NULL | deliberate on Java — contract says null until processing begins |
| received_at | timestamptz NOT NULL DEFAULT now() | timestamptz NOT NULL | |
| shoe_id | varchar(40) | varchar(40) | |
| status | varchar(12) NOT NULL DEFAULT 'queued' | varchar(12) NOT NULL | |
| stored_path | varchar(255) NOT NULL DEFAULT '' | varchar(255) NOT NULL | |
| weight_after_kg | double precision | double precision | |
| weight_before_kg | double precision | double precision | |

### `upload_batch` (Java) vs `uploads_uploadbatch` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| athlete_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| completed_at | timestamptz | timestamptz | |
| error_code | varchar(100) NOT NULL DEFAULT '' | varchar(100) NOT NULL | |
| error_message | varchar(500) NOT NULL DEFAULT '' | varchar(500) NOT NULL | |
| filename | varchar(255) NOT NULL | varchar(255) NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| on_duplicate | varchar(10) NOT NULL DEFAULT 'skip' | varchar(10) NOT NULL | |
| received_at | timestamptz NOT NULL DEFAULT now() | timestamptz NOT NULL | |
| status | varchar(12) NOT NULL DEFAULT 'unpacking' | varchar(12) NOT NULL | |

### `webhook` (Java) vs `webhooks_webhook` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| created | timestamptz NOT NULL DEFAULT now() | timestamptz NOT NULL | |
| events | jsonb NOT NULL DEFAULT '[]' | jsonb NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| owner_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| secret | varchar(64) NOT NULL | varchar(64) NOT NULL | |
| status | varchar(10) NOT NULL DEFAULT 'active' | varchar(10) NOT NULL | |
| url | varchar(2048) NOT NULL | varchar(200) NOT NULL | Java allows longer URLs |

### `webhook_delivery` (Java) vs `webhooks_webhookdelivery` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| attempts | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| created | timestamptz NOT NULL DEFAULT now() | timestamptz NOT NULL | |
| event | varchar(50) NOT NULL | varchar(50) NOT NULL | |
| id | bigint (sequence) | bigint | |
| last_error | text NOT NULL DEFAULT '' | text NOT NULL | |
| payload | jsonb NOT NULL | jsonb NOT NULL | |
| status | varchar(10) NOT NULL DEFAULT 'pending' | varchar(10) NOT NULL | |
| webhook_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |

### `workout` (Java) vs `workouts_workout` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| created_by / created_by_id | `created_by` | `created_by_id` | Django auto-suffixes FK columns with `_id`; Java's JPA mapping didn't here |
| duration | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| id | varchar(40) NOT NULL | varchar(40) NOT NULL | |
| name | varchar(200) NOT NULL | varchar(200) NOT NULL | |
| sport | varchar(10) NOT NULL | varchar(10) NOT NULL | |
| tss | integer NOT NULL DEFAULT 0 | integer NOT NULL | |
| type | varchar(50) NOT NULL DEFAULT '' | varchar(50) NOT NULL | |

### `workout_step` (Java) vs `workouts_workoutstep` (Python)

| Column | Java | Python | Note |
|---|---|---|---|
| distance | integer | integer | |
| duration | integer | integer | |
| end_type | varchar(10) NOT NULL | varchar(10) NOT NULL | |
| id | bigint (sequence) | bigint | |
| kind | varchar(10) NOT NULL | varchar(10) NOT NULL | |
| order / step_order | `step_order` | `order` | renamed only — `order` is a SQL reserved word, Java avoided it |
| repeat | integer NOT NULL DEFAULT 1 | integer NOT NULL | |
| target_pct | double precision | double precision | |
| workout_id | varchar(40) NOT NULL | varchar(40) NOT NULL | |

## Structural differences (not just naming)

- **OAuth schema.** Java's `oauth_authorization` is one custom table backing a hand-rolled
  `OAuth2AuthorizationService`. Python uses `django-oauth-toolkit`'s own 5-table schema
  (`oauth2_provider_accesstoken`, `_application`, `_grant`, `_idtoken`, `_refreshtoken`).
  Same external behavior (`cad_at_*`/`cad_rt_*` tokens, same grant types), different
  internal storage model entirely.
- **`record` primary key.** Java uses a composite `(activity_id, ts)` key on the
  TimescaleDB hypertable directly — no surrogate id, since nothing ever looks up a record
  by its own id. Python keeps a `bigint` surrogate `id` alongside the hypertable
  partitioning column.

## Tables that exist on only one side

Both sides have tables with no equivalent at all — these are framework infrastructure,
not product schema, so they're not really comparable to anything:

**Java only:**
- `flyway_schema_history` — Flyway's own migration ledger
- `batch_job_instance`, `batch_job_execution`, `batch_job_execution_context`,
  `batch_job_execution_params`, `batch_step_execution`, `batch_step_execution_context` —
  Spring Batch's job-tracking tables (Celery doesn't need equivalents in the app DB)

**Python only:**
- `django_migrations`, `django_content_type`, `django_admin_log`, `django_session` —
  Django's own bookkeeping
- `auth_group`, `auth_permission`, `auth_group_permissions`,
  `accounts_user_groups`, `accounts_user_user_permissions` — Django's built-in
  groups/permissions framework (the product itself uses its own `UserRelationship`-based
  permission checks, not this)
- `oauth2_provider_accesstoken`, `_application`, `_grant`, `_idtoken`, `_refreshtoken` —
  see "Structural differences" above
